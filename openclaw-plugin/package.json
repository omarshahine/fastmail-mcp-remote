{
  "name": "fastmail-cli",
  "version": "3.2.1",
  "description": "OpenClaw plugin for Fastmail email, contacts, and calendar",
  "type": "module",
  "files": [
    "bin",
    "index.ts",
    "src",
    "skills",
    "openclaw.plugin.json",
    "README.md",
    "dist/"
  ],
  "keywords": [
    "openclaw",
    "fastmail",
    "email",
    "agent-tools"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/omarshahine/fastmail-mcp-remote.git",
    "directory": "openclaw-plugin"
  },
  "peerDependencies": {
    "openclaw": ">=2026.6.5"
  },
  "peerDependenciesMeta": {
    "openclaw": {
      "optional": true
    }
  },
  "devDependencies": {
    "@types/node": "^26.4.0",
    "openclaw": ">=2026.6.5",
    "typescript": "^7.0.2",
    "tsup": "^8.5.0"
  },
  "openclaw": {
    "extensions": [
      "./index.ts"
    ],
    "compat": {
      "pluginApi": ">=2026.6.5"
    },
    "build": {
      "openclawVersion": "2026.6.5",
      "pluginSdkVersion": "2026.6.5"
    }
  },
  "scripts": {
    "plugin:check": "npx --yes @openclaw/plugin-inspector inspect --no-openclaw",
    "plugin:ci": "npx --yes @openclaw/plugin-inspector ci --no-openclaw --runtime --mock-sdk --allow-execute",
    "build": "tsup index.ts --format esm --out-dir dist --clean --target node22 && tsup --entry.cli ../cli/main.ts --format cjs --out-dir dist --target node22 --no-splitting",
    "test:pack": "npm run build && node test/pack-smoke.mjs",
    "prepack": "npm run build"
  },
  "bin": {
    "fastmail": "./bin/fastmail.js"
  },
  "main": "./dist/index.js",
  "engines": {
    "node": ">=22.19.0"
  }
}
