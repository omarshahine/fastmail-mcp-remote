{
  "id": "fastmail-cli",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "cliCommand": {
        "type": "string",
        "default": "fastmail",
        "description": "Path or alias for the fastmail CLI (default: 'fastmail')"
      }
    }
  },
  "skills": [
    "./skills"
  ]
}
